{
  "description": "",
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-organize-attributes": "^1.0.0",
    "prettier-plugin-sort-imports": "^1.8.9",
    "prettier-plugin-sort-json": "^4.2.0",
    "prettier-plugin-sort-members": "^0.2.4",
    "prettier-plugin-tailwindcss": "^0.7.2"
  },
  "main": "index.js",
  "name": "BlogPreviewCard",
  "packageManager": "pnpm@10.23.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r --parallel build",
    "clean": "pnpm -r --parallel clean",
    "dev": "pnpm -r --parallel dev",
    "prod": "pnpm -r --parallel prod",
    "test": "pnpm -r --parallel test",

    "springboot:build": "pnpm run --filter=springboot build",
    "springboot:clean": "pnpm run --filter=springboot clean",
    "springboot:dev": "pnpm run --filter=springboot dev",
    "springboot:prod": "pnpm run --filter=springboot prod",
    "springboot:test": "pnpm run --filter=springboot test",

    "react:build": "pnpm run --filter=react build",
    "react:clean": "pnpm run --filter=react clean",
    "react:dev": "pnpm run --filter=react dev",
    "react:prod": "pnpm run --filter=react prod",
    "react:test": "pnpm run --filter=react test"
  },
  "version": "1.0.0"
}
